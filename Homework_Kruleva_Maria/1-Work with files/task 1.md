### Работа в консольке

1. **Переместиться между директориями**
	Для перемещения из одной папки в другую используется команда cd, название команды происходит от change directory. Синтаксис у команды такой:
	`cd путь к директории`.
	
	Путь может быть абсолютным и относительным.
	**Абсолютный путь**: <img width="670" height="490" alt="Image" src="https://github.com/user-attachments/assets/faaf92ef-a0f5-4631-a5c0-9e455da4e2a7" />
	**Относительный путь:**
	<img width="669" height="491" alt="Image" src="https://github.com/user-attachments/assets/2c6e5732-3f3e-46e3-82f1-9df363e079c4" />
2. **Вывести список файлов в директории**
	С помощью команды `ls` мы сможем увидеть список файлов в директории, например, в домашней директории:
	
	<img width="661" height="486" alt="Image" src="https://github.com/user-attachments/assets/39fd5540-4273-4f9d-89d2-df94606e7081" />
	
	Но мне хочется показать, как будет выглядеть и не в домашней директории: для наглядности создадим в папке `homework` (была создана с помощью команды `mkdir`) файлы `task1`, `task2,` `maria.txt`, `domashka.py` с помощью команды `touch`. 
	Команда `ls` выводит список файлов и подкаталогов, которые находятся в нашей директории. 
	Вот такой результат в этом случае:
	
	![[Pasted image 20251021213732.png]]
3. **Вывести список Всех файлов в директории**
	Я так поняла это задание, что нужно вывести список всех файлов в директории в том числе и скрытых. (надеюсь, правильно поняла)
	
	Для этого в команде `ls` используется флаг `-a` - этот файл выводит все скрытые файлы. В моем случае они такие:
	![[Pasted image 20251021215644.png]]
	
4.  **Создать папку с подпапками**
	Вернемся в домашнюю директорию с помощью команды `cd ~`. Там я создам новую папку `test`, в которой создам папки `dir1`, `dir2`, `lalala`, `trololo`, `tralala` при помощи команды `mkdir`. Вот так это будет выглядеть:
	![[Pasted image 20251021215042.png]]
5. **Внутри папки создать файлик и записать в него что-нибудь**
	Я вернулась в папку `homework`, там есть файлик `maria.txt,` туда я и запишу что-нибудь.
	
	На практическом занятии мы использовали для этого редактор `vim`, поэтому будем пользоваться именно им, хотя есть текстовый редактор `nano` 
	
	`vim maria.txt` - открывается редактор
	чтобы перейти в режим вставки, нужно нажать клавишу `i`, там я пишу текст и чтобы сохранить файл с очень важным содержанием, который нельзя забывать, ввожу `:wq`. Так выглядит текстовый файл с **очень важной** информацией:
	
	![[Pasted image 20251021220749.png]]
	
6. **Переместить файл из одной директории в другую**
	Переместим файл `lalala.txt` (я его создала, можно увидеть на скрине) из директории `test` в `homework` для этого использую команду `mv`:
	
	![[Pasted image 20251021221704.png]]
	Мы переместили файлы и смотрим с помощью команды `ls`, что в папке `homework` `lalala.txt` появился, а в папке `test` его нет, значит, все хорошо и переместить файлик удалось.
7. **Скопировать файл из одной директории в другую**
	Мне очень хочется, чтобы файл `lalala.txt` был в двух папках сразу, поэтому использую команду `cp`, чтобы скопировать его в директорию `test` 
	
	Команда выглядит примерно так: `cp исходный_файл целевая_директория` 
	
	В моем случае, я буду писать относительный путь к моему файлику `lalala.txt`, находящий в папке `homework` и буду копировать в папку `test`. На скрине видно, как я это сделала. Далее я проверяла, все ли у меня получилось: и в папке `test` и в папке `homework` есть файл `lalala.txt`, а значит, задание выполнено!
	
	![[Pasted image 20251021222727.png]]
8. **Переименовать файл**
	Название `domashka.py` мне не нравится, поэтому пришло время переименовать этот файл. Будем обычными людьми, поэтому файл станет тоже обычным - `main.py`. Для этого буду использовать команду `mv`:
	`mv старое_имя новое_имя`
	
	![[Pasted image 20251021223653.png]]
9. **Сравнить содержимое файла**
	Для сравнения файлов используется команда `diff`, которая показывает различия построчно. `diff файл1 файл 2`. 
	
	Для интереса, я создам файл misha.txt и запишу туда "Меня зовут Миша", далее сравним эти два файла:
	получаем `1c1` это значит, что первая строка изменена между файлами.
	Но мой интерес не пропал. Я создала файлик text.txt, в котором написала:
	```
	ТЕКСТ
	
	```
	Здесь вывело: `1c1, 2` - одна строка из первого файла была заменена на две строки во втором файле.
	![[image (4).png]]
10. **Отсортировать содержимое файла по возрастанию и убыванию**
	Для этого создам новый файлик, в который помещу какой-нибудь короткий анекдот:
	![[image (5).png]]
	Теперь с помощью команды `sort` отсортируем по возрастанию в алфавитном порядке:
	`sort имя_файла`
	И анекдот превращается в несмешной набор слов :(
	![[image (6).png]]
	Чтобы отсортировать файл по убыванию нужно использовать флаг `-r`
	Отсортировывать по убыванию я буду текстовый файл с числами, так интереснее и нагляднее. Чтобы отсортировать наш файл как числа, а не как строки, используется флаг `-n`. Чтобы отсортировать как числа и по убыванию эти флаги сливаются и получается  флаг `-nr`. 
	
	Вот такие числа я записала в текстовый файл `nums.txt`:
	![[image (7).png]]
	Вот какой ввод и вывод был в терминале:
	![[image (8).png]]
	Действительно, все числа отсортировались по убыванию правильно, как числа, а не как строки.
1. **Удалить все папки и файлы**
	==*Здесь я покажу **2 способа** удаления папок:*==
	
	Чтобы удалить директорию со всем ее содержимым, я использую команду `rm` с ключом `-r`. 
	
	Ключ `-r` отвечает за рекурсивное удаление файлов и директорий. То есть команда удаления будет последовательно применяться к каждому из элементов, находящихся внутри указанной директории, пока наконец не сотрёт их все. После чего удалит и опустевшую к тому моменту директорию.
	![[Pasted image 20251021232736.png]]
	![[Pasted image 20251021232749.png]]
	
	Но если в директории файлов будет очень много, этот вариант не подходит, потому что удаление этой директории может затянуться на очень долгое время. **Нужно действовать радикальнее**. Поэтому следующую директорию я удалю **другим способом**:
	`rm -rf имя папки`
	![[Pasted image 20251021232947.png]]
	Если мы вернемся к скрину, который я оставила в задании, то увидим, что там были папки `homework`, `test`. Больше их нет. 
	
	Чтобы удалить файлы в конкретной папке нужно поступить следующим образом:
	`rm /путь/к_папке/*`
