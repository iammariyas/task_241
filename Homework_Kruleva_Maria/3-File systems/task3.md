# Продолжаем

1. **Выведите содержимое fstab. Что хранится в fstab?**
	***File System Table (fstab)*** - это конфигурационный файл, который определяет, какие файловые системы должны монтироваться автоматически при загрузке системы. Это важный элемент для любого администратора сервера, будь то физический сервер или виртуальный сервер (VPS). 
	
	обычно находится в `/etc/fstab` и состоит из строк, каждая из которых описывает отдельную файловую систему. Стандартная строка в `fstab` имеет следующий формат: `<файловая система> <точка монтирования> <тип> <опции>`.
	
	Содержимое `fstab` можно посмотреть с помощью команды `cat /etc/fstab`
	
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229214210.png)

2. **Добавьте в виртуальную машину ещё один диск**
	Это нельзя сделать внутри виртуальной машины, к сожалению. Нужно либо выключить машину и сделать в управлении VirtualBox, либо через терминал хостовой системы. А уже дальше нужно внутри виртуальной машины обнаружить новый диск после добавления, разметить и отформатировать его, а затем смонтировать и настроить.
	
	Буду делать через VirtualBox. 
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229222023.png)
	
	В виртуальной машине проверяем, все ли хорошо, обнаружила ли машина новый диск. я использую команду `fdisk -l` и действительно обнаруживает:
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229222937.png)
	sda2 - это новый диск на 5Гб
3. **Узнайте как система видит ваш диск - выведите информацию о блочных устройствах**
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229224403.png)

	sdb - мой новый диск
	
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229224502.png)
4. **С помощью полученной информации создайте на диске таблицу разделов и фаловую систему ext4**
	Здесь в целом ничего не меняется, делаем точно также как в task1, только теперь пишем в качестве имени диска sdb
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229224741.png)
5. **Примонитруте диск в каталог /mnt**
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229225040.png)
	Делаем точно также, как мы уже делали в задании task1. Проверим, что мы вмонтировали:
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229225122.png)
	
6. **Зайдите в каталог и создайте там файлы**
	Создаем файлы с помощью команды `touch`.  у меня это файлы `file1.txt, file2.txt, file3.txt` и проверяем, что они точно создались, а затем смотрим, что файлы находятся на нужном диске с помощью `df -h .` - вывод покажет, что текущий каталог (`.`) смонтирован в /dev/sdb.
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229225420.png)
7. **Отмонтируйте диск и проверье остались ли файлы**
	Файлы, которые мы создавали не будут видны, потому что они находились на диске, а не в этом каталоге ФС хоста. если мы снова смонтируем диск и проверим файлы, то они снова появятся.
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229225621.png)
8. **Сделайте так чтобы диск автоматически подключался при загрузке систем ( добавьте информацию о нём с fstab)**
	Сначала нужно узнать UUID раздела с помощью команды `blkid /dev/sdb`, текст будет выглядеть примерно так:
	`/dev/sdb1: UUID="номер UUID" TYPE="ext4"`, затем в конец файла `nano /etc/fstab` дописываю: 
	`UUID=номер_UUID  /mnt/mydisk  ext4  defaults  0  2`. После этого перезагружаю систему.  И сразу после этого ввожу команду `df -h /mnt/mydisk` и диск уже должен быть смонтирован. 
	
9. **Проверьте корретность записанных в fstab данных перед перезагрузкой**
	```
	mount -a
	```
	Команда `mount -a` монтирует все файловые системы из `/etc/fstab`. Если есть ошибки - они будут показаны.
10. **Перезагрущите систему и убедитесь что диск был подключён к системе**
	Я это сделала в 6 пункте. перезагрузка осуществляется с помощью команды `reboot`. затем ввожу команду в терминал `df -h /mnt/mydisk` и получаю:
	
	![alt text](https://github.com/iammariyas/task_241/blob/labs/Homework_Kruleva_Maria/image/Pasted%20image%2020251229232041.png)
	
